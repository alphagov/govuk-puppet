---

govuk::node::s_base::apps:
  - email_campaign_frontend

govuk_rbenv::rubies: 'all'

lv:
  data:
    pv: '/dev/sdb1'
    vg: 'vhost'

mount:
  /data/vhost:
    disk: '/dev/mapper/vhost-data'
    govuk_lvm: 'data'
    mountoptions: 'defaults'

---
govuk_crawler::mirror_root: '/mnt/crawler_worker'

lv:
  worker:
    pv: '/dev/sdc1'
    vg: 'crawler'

mount:
  /mnt/crawler_worker:
    disk: '/dev/mapper/crawler-worker'
    govuk_lvm: 'worker'
    mountoptions: 'defaults'

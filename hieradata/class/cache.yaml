---

govuk::apps::router::mongodb_name: 'router'
govuk::apps::router::mongodb_nodes:
  - 'router-backend-1.router'
  - 'router-backend-2.router'
  - 'router-backend-3.router'

govuk::apps::authenticating_proxy::mongodb_nodes:
  - 'router-backend-1.router'
  - 'router-backend-2.router'
  - 'router-backend-3.router'

nginx::package::nginx_package: 'nginx-extras'

# NOTE: These values should not be changed lightly once traffic replay is
# enabled using router::gor::enable_staging.
#
# If changed, the old host record must be cleaned up; see the FIXME in
# router::gor.
router::gor::staging_host: "www-origin-staging.%{hiera('app_domain')}"
router::gor::staging_ip: '37.26.91.14'

<%
# Sort by key name/comment to ensure ordering on older Rubies.
@ssh_keys.sort_by { |k,v| k }.each do |name, key|
-%>
<%= '#' if key == 'NONE_IN_HIERA' -%>ssh-rsa <%= key -%> <%= name %>
<% end -%>

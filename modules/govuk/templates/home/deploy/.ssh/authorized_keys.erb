<%
# Sort by key name/comment to ensure ordering on older Rubies.
@deploy_ssh_keys.sort_by { |k,v| k }.each do |name, key|
-%>
<%= '#' if key == 'NONE_IN_EXTDATA' -%>ssh-rsa <%= key -%> <%= name %>
<% end -%>

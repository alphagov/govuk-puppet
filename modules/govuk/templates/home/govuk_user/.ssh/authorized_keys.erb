<%
# Cast single strings as arrays so that we can iterate.
key_lines = [@ssh_key].flatten
# Prevent order of file changing between runs.
key_lines.sort!
key_lines.each do |key_line|
-%>
<%= key_line %>
<% end -%>

server {
  listen 80;
  server_name deploy.*;

  location / {
    proxy_pass http://localhost:8080;
  }
}

server {
  listen 80;
  server_name puppetdb.*;

  location / {
    proxy_pass http://puppet-1.management/;
  }
}

define command {
  # HOSTADDRESS = IP 
  # ARG1 = Hostname to supply to Webserver (www.gov.uk)
  # ARG2 = Warn if no response in this time (seconds)
  # ARG3 = Critical if no response in this time (seconds)
  command_name  check_http_port
  command_line  /usr/lib/nagios/plugins/check_http -I $HOSTADDRESS$ -w $ARG2$ -c $ARG3$ -H $ARG1$
}
define command {
  # HOSTADDRESS = IP 
  # ARG1 = Hostname to supply to Webserver (www.gov.uk)
  # ARG2 = Warn if no response in this time (seconds)
  # ARG3 = Critical if no response in this time (seconds)
  command_name  check_https_port
  command_line  /usr/lib/nagios/plugins/check_http -I $HOSTADDRESS$ -a "<%= @http_username %>:<%= @http_password %>" -w $ARG2$ -c $ARG3$ -S -H $ARG1$
}
define command {
  # HOSTADDRESS = IP 
  # ARG1 = Hostname to supply to Webserver (www.gov.uk)
  # ARG2 = Warn if no response in this time (seconds)
  # ARG3 = Critical if no response in this time (seconds)
  # ARG4 = A URL to GET or POST
  command_name  check_https_url
  command_line  /usr/lib/nagios/plugins/check_http -I $HOSTADDRESS$ -a "<%= @http_username %>:<%= @http_password %>" -w $ARG2$ -c $ARG3$ -S -H $ARG1$ -u $ARG4
}

define service {
  use                       govuk_regular_service
  host_name                 <%= @host_name %>
  service_description       <%= @service_description %>
  passive_checks_enabled    1
  check_period              never
  check_command             check_dummy!1!"Unexpected active check on passive service"
  max_check_attempts        1
}

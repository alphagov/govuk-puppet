define command {
  command_name check_whitehall_overdue
  command_line /usr/lib/nagios/plugins/check_http -H <%= @whitehall_hostname %> -u '<%= @whitehall_overdue_url %>' -a <%= @http_username %>:<%= @http_password %> -s '{"overdue":0}'
}

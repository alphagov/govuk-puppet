---
- scm:
    name: smokey_Smokey_Production
    scm:
        - git:
            url: git@github.com:alphagov/smokey.git
            branches:
              - master

- job:
    name: Smokey_Production
    display-name: Smokey_Production
    project-type: freestyle
    scm:
      - alphagov-deployment_Production_Deploy
    builders:
        - shell: |
            set +x
            export TARGET_PLATFORM=production
            export AUTH_USERNAME=betademo
            export AUTH_PASSWORD=nottobesharedout
            export MYSQL_USERNAME=panopticon
            export EFG_DOMAIN='https://www.sflg.gov.uk'
            export EFG_USERNAME='support@alphagov.co.uk'
            export EFG_PASSWORD='<%= @efg_password %>'
            export SIGNON_EMAIL='<%= @signon_email %>'
            export SIGNON_PASSWORD='The Quick Brown Fox1!'
            export BEARER_TOKEN=<%= @smokey_bearer_token %>
            export RATE_LIMIT_TOKEN=<%= @smokey_rate_limit_token %>
            set -x
            ./jenkins.sh
    wrappers:
        - ansicolor:
            colormap: xterm

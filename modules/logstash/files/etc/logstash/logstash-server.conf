input {
  amqp {
    type      => "all"
    host      => "localhost"
    exchange  => "rawlogs"
    name      => "rawlogs_consumer"
    exclusive => false
  }
  amqp {
    type      => "all"
    host      => "localhost"
    exchange  => "rawlogs"
    name      => "rawlogs_consumer"
    exclusive => false
  }
  amqp {
    type      => "all"
    host      => "localhost"
    exchange  => "rawlogs"
    name      => "rawlogs_consumer"
    exclusive => false
  }
}

output {
  elasticsearch {
    cluster => "logstash-server"
    host    => "localhost"
    port    => "9391"
  }
}

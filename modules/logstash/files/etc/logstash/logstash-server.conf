input {
  amqp {
    type => "all"
    host => "localhost"
    exchange => "rawlogs"
    name => "rawlogs_consumer"
  }
}

output {
   stdout { debug => true }

  # If your elasticsearch server is discoverable with multicast, use this:
  elasticsearch { }

}
